import { GetSubpageLinkPipe } from './get-subpage-link.pipe';

describe('GetSubpageLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GetSubpageLinkPipe();
    expect(pipe).toBeTruthy();
  });
});